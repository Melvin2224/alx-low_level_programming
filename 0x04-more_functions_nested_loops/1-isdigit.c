#include "main.h"


/**
 * _isdigit - This function, checks if variable value is a digit
 * @c: first operrand
 *
 * Return: returns 1 is value is a digit and 0 if now
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
