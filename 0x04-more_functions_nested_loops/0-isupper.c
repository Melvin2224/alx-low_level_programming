#include "main.h"

/**
 * _isupper - This function, checks if a varaible value is in upper case.
 * @c: first parameter
 *
 * Return: returns 1 if c is uppercase and 0 if not
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
