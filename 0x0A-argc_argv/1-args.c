#include "main.h"
#include <stdio.h>

/**
 * main - displays the number of arguments passed into it
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: 0 if successful
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
