#include "main.h"

/**
 * int _add - Finds the sum of two integers
 * @num: First parameter
 * @num1: second parameter
 *
 * Return: Returns the sum of the two integers;
 */
int _add(int num, int num1)
{
	return (num + num1);
}
